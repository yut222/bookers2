<div class="container px-5 px-sm-0">
  <div class="row">

    <!-- col3部分を共通化 -->
    <%= render 'users/list', user: @user, book: @book %>

    <!-- ユーザーの投稿詳細 -->
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
          <tbody>
            <tr>
              <tbody>
                <tr>
                  <td><%= image_tag @book_show.user.get_profile_image(100,100) %><%= link_to @book_show.user.name, user_path(@book_show.user.id) %><%= @book_show.user.name %></td>
                  <td><%= link_to @book_show.title, "book_path(book.id)" %></td>
                  <td><%= @book_show.body %></td>

                </tr>
              </tbody>
            </tr>
          </tbody>
      </table>
    </div>

  </div>
</div>