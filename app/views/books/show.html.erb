<!-- フラッシュメッセージ -->
  <% flash.each do |message_type, message| %>
    <%= content_tag :div, message, class: message_type %>
  <% end %>


<div class="container px-5 px-sm-0">
  <div class="row">

    <!-- col3部分を共通化 -->
    <%= render 'users/list', user: @user, book: @book %>


    <!-- ユーザーの投稿詳細 -->
    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">


          <tbody>
            <tr>

               <tbody>
                <tr>
                  <td><%= image_tag @books.user.get_profile_image(100,100) %><%= link_to @books.user.name, "user_path" %></td>
                  <td><%= user.name %></td>
                  <td><%= link_to @books.title, "book_path(book.id)" %></td>
                  <td><%= @books.body %></td>
                </tr>
               </tbody>

              </td>
            </tr>
          </tbody>





      </table>
    </div>
  </div>
</div>